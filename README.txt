Connect you Nibe devices that uses the myUplink API.

Tested on Nibe S1255. Most importent datapoints is represented in app. Most of the standard datapoints available in insights.

At current state, the app do NOT support addon peripherals like ventilation and pool heaters.